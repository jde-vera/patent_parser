# patent_parser
- This project uses the NLP library spaCy to analyse the structure of a patent claim and demonstrates how computational tools can assist patent attorneys in reviewing claims
# How it works
- you first have to install the requirements by typing in the terminal `pip install -r requirements.txt`
- then you have to install the language model `python -m spacy install en_core_web_sm`